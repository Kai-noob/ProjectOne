import 'package:flutter/material.dart';

class TestDesktopLandscape extends StatelessWidget {
  const TestDesktopLandscape({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}


class TestDesktopPortrait extends StatelessWidget {
  const TestDesktopPortrait({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}